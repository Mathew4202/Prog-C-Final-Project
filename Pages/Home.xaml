<UserControl x:Class="Final_Project.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE: INTRO TEXT -->
        <StackPanel Grid.Column="0"
                    VerticalAlignment="Center"
                    Margin="20">

            <TextBlock Text="Welcome to the Movie Review Application"
                       FontSize="28"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Margin="0 0 0 20"/>

            <TextBlock Text="This application allows users to browse movies and submit their own reviews."
                       FontSize="16"
                       TextWrapping="Wrap"
                       Margin="0 0 0 10"/>

            <TextBlock Text="You can explore four main pages:"
                       FontSize="16"
                       Margin="0 0 0 10"/>

            <TextBlock Text="• Home Page" FontSize="14"/>
            <TextBlock Text="• Movies Page" FontSize="14"/>
            <TextBlock Text="• Popular Movies Page" FontSize="14"/>
            <TextBlock Text="• Review Page" FontSize="14"/>
        </StackPanel>

        <!-- RIGHT SIDE: BUTTONS -->
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="200"
                    Margin="20">

            <Button Content="let's go!"
                    Height="40"
                    FontSize="16"
                    Margin="0 0 0 10"
                    Command="{Binding GoToMoviesCommand}"/>
        </StackPanel>
    </Grid>

</UserControl>
